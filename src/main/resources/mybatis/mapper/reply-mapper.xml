<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.mapper.ReplyMapper">
<select id="replyListData" resultType="ReplyVO" parameterType="hashmap">
SELECT no,cno,type,id,name,msg,TO_CHAR(regdate,'YYYY-MM-DD HH24:MI:SS') as dbday FROM comment_2 WHERE cno=#{cno} AND type=#{type} ORDER BY no DESC
</select>
</mapper>
